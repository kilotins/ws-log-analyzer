services:
  logpilot:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - uploads:/app/uploads
      - reports:/app/reports
      - cache:/app/cache
    restart: unless-stopped
    labels:
      - "traefik.http.services.http-0-tkwc0hbbubaobperbq682q9k-logpilot.loadbalancer.server.port=8501"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  uploads:
  reports:
  cache:
