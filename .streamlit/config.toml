[theme]
# Item Consulting brand — works in both light and dark mode.
# Users toggle via Settings > Theme or system preference.
primaryColor = "#7C3AED"
font = "sans serif"

[server]
maxUploadSize = 200
